# Production Environment Variables
# Production Ortam Değişkenleri
#
# This file is for reference only. In production, set these as environment variables/secrets.
# Bu dosya sadece referans içindir. Production'da bunları environment variable/secret olarak ayarlayın.

# Server Configuration
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Firebase Configuration (REQUIRED)
# IMPORTANT: Use SERVICE_ACCOUNT_JSON in production, not SERVICE_ACCOUNT_PATH
SERVICE_ACCOUNT_JSON="<base64_encoded_json_string>"
FIREBASE_PROJECT_ID=veratoptan-c4d30
FIREBASE_PROJECT_NUMBER=989392397922

# CORS Configuration
# In production, specify allowed origins instead of '*'
CORS_ORIGINS=https://your-expo-app.com,https://your-domain.com
CORS_CREDENTIALS=true

# Logging Configuration
LOG_REQUESTS=true
LOG_LEVEL=info

# Security Configuration
MAX_REQUEST_SIZE=10
RATE_LIMIT=60
